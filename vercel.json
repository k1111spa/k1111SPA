{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && pnpm run prisma:seed && next build",
  "devCommand": "pnpm run dev",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
